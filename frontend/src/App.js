import React from 'react';
import './App.css';
function App(){
  return <div style={{padding:20}}>
    <h1>Community Savings — Frontend (Starter)</h1>
    <p>This React app is a minimal scaffold. Use it to call backend APIs from /api/</p>
  </div>
}
export default App;
